description = "Apereo Central Authentication Service - cbontemps overlay"

allprojects {
    apply plugin: 'java'
}

task wrapper(type: Wrapper, description: "Gradle's wrapper") {
    gradleVersion = "2.10"
}